#include<stdio.h>

int main()
{
    int n;
    char ch;
    float k;
    double r;
    printf("int %d\n",sizeof(n));
    printf("char %d\n",sizeof(ch));
    printf("float %d\n",sizeof(k));
    printf("double %d",sizeof(r));
    return 0;

}